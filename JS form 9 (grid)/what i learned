Setting body height 'height:100vh' can cause content taller than the viewport to be cut off, preventing scrolling to the top.
The issue was resolved by using min-height:100vh on the body.
which ensures the container is at least the height of the viewport but can grow with its content, allowing natural scrolling on smaller screens.
